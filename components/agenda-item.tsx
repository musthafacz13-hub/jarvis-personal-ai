import { Pressable, StyleSheet, Text, View } from "react-native";
import { IconSymbol } from "@/components/ui/icon-symbol";
import type { AgendaItem } from "@/lib/jarvis-types";

function timeLabel(value: string | null) { return value ? new Date(value).toLocaleString("en-US", { weekday: "short", hour: "numeric", minute: "2-digit" }) : "No due time"; }
export function AgendaRow({ item, onPress }: { item: AgendaItem; onPress?: () => void }) { const event = item.kind === "event"; return <Pressable onPress={onPress} disabled={!onPress} style={({ pressed }) => [styles.row, pressed && styles.pressed]}><View style={[styles.iconWrap, event ? styles.eventIcon : styles.reminderIcon]}><IconSymbol name={event ? "calendar" : "checkmark.circle"} size={18} color={event ? "#67E8F9" : "#34D399"} /></View><View style={styles.copy}><Text style={styles.title} numberOfLines={1}>{item.title}</Text><Text style={styles.time}>{timeLabel(item.startsAt)}</Text></View><Text style={styles.kind}>{event ? "EVENT" : "TASK"}</Text></Pressable>; }
const styles = StyleSheet.create({ row: { flexDirection: "row", alignItems: "center", gap: 12, paddingVertical: 13 }, pressed: { opacity: 0.7 }, iconWrap: { width: 38, height: 38, borderRadius: 19, alignItems: "center", justifyContent: "center" }, eventIcon: { backgroundColor: "#15314A" }, reminderIcon: { backgroundColor: "#12352E" }, copy: { flex: 1, gap: 3 }, title: { color: "#F8FAFC", fontSize: 15, fontWeight: "600" }, time: { color: "#94A3B8", fontSize: 12 }, kind: { color: "#64748B", fontSize: 10, fontWeight: "700", letterSpacing: 0.8 }, });
