import { RefreshControl, ScrollView, StyleSheet, Text, View } from "react-native";
import { AgendaRow } from "@/components/agenda-item";
import { ScreenContainer } from "@/components/screen-container";
import { useJarvis } from "@/lib/jarvis-context";
export default function AgendaScreen() { const { agenda, isRefreshing, refresh, permissions } = useJarvis(); const events = agenda.filter((item) => item.kind === "event"); const reminders = agenda.filter((item) => item.kind === "reminder"); return <ScreenContainer className="px-5"><ScrollView refreshControl={<RefreshControl refreshing={isRefreshing} onRefresh={refresh} tintColor="#67E8F9" />} contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><Text style={styles.eyebrow}>YOUR NEXT 14 DAYS</Text><Text style={styles.title}>Agenda</Text><Text style={styles.subhead}>Pull down to refresh your permitted iPhone data.</Text><Text style={styles.label}>CALENDAR</Text><View style={styles.card}>{events.map((item, index) => <View key={item.id} style={index ? styles.divider : undefined}><AgendaRow item={item} /></View>)}{!events.length && <Text style={styles.empty}>{permissions.calendar === "granted" ? "No events are scheduled in the next 14 days." : "Allow Calendar access in Settings to see your events."}</Text>}</View><Text style={styles.label}>REMINDERS</Text><View style={styles.card}>{reminders.map((item, index) => <View key={item.id} style={index ? styles.divider : undefined}><AgendaRow item={item} /></View>)}{!reminders.length && <Text style={styles.empty}>{permissions.reminders === "granted" ? "No open reminders due in the next 14 days." : "Allow Reminders access in Settings to see your tasks."}</Text>}</View></ScrollView></ScreenContainer>; }
const styles = StyleSheet.create({ content: { paddingTop: 16, paddingBottom: 28, gap: 14 }, eyebrow: { color: "#38BDF8", fontSize: 10, fontWeight: "800", letterSpacing: 1.2 }, title: { color: "#F8FAFC", fontSize: 32, lineHeight: 40, fontWeight: "700" }, subhead: { color: "#94A3B8", fontSize: 14, lineHeight: 20, marginBottom: 12 }, label: { color: "#B7C8D9", fontSize: 11, fontWeight: "800", letterSpacing: 1, marginTop: 4 }, card: { backgroundColor: "#101A33", borderRadius: 18, borderWidth: 1, borderColor: "#263553", paddingHorizontal: 15 }, divider: { borderTopWidth: 1, borderTopColor: "#263553" }, empty: { color: "#94A3B8", fontSize: 14, lineHeight: 20, paddingVertical: 22 } });
